# TODO - Run Laravel locally

- [ ] Create `backend/.env` from `backend/.env.example`
- [ ] Install backend PHP dependencies: `cd backend && composer install`
- [ ] (Optional for assets) Install frontend JS dependencies: `cd backend && npm install`
- [ ] Generate Laravel key: `cd backend && php artisan key:generate`
- [ ] Run Laravel dev server: `cd backend && php artisan serve`
- [ ] Visit URL printed by `php artisan serve`

- [ ]cmd /c "cd /d C:\Users\Administrator\Documents\InventoryTracking\backend && php artisan serve"